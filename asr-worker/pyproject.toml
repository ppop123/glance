[project]
name = "fanyi-asr-worker"
version = "0.1.0"
description = "MLX Whisper ASR worker (runs on Mac mini)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
    "mlx-whisper>=0.4",
    "numpy>=1.26",
]
